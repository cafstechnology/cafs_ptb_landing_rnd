import React, { useEffect, useState } from 'react';
import { Layout } from 'antd';

const App = ({ history }) => {
    return (
        <Layout style={{ position: 'sticky', zIndex: 1, width: '100%' }}>
            <Layout>
            </Layout>
        </Layout>
    );
}

export default App;
